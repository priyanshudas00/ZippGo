# TODO: Clean Remote GitHub Repo and Upload New Code

- [ ] Check if GitHub CLI (gh) is installed; install if not
- [ ] Clone the remote repo https://github.com/priyanshudas00/ZippGo to a temp directory
- [ ] In temp directory, remove all files (git rm -r .), commit, push to clean the repo
- [ ] Copy all files from current local directory to temp directory
- [ ] Add, commit, push the new code to the remote repo
- [ ] Verify the remote repo is updated with new code
